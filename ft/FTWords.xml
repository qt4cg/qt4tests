<test-set xmlns="http://www.w3.org/2010/09/qt-fots-catalog" name="ft-FTWords">
  <description>XQuery Full Text - Full-Text Tokens and Phrases - Section 3.2.</description>
  <dependency type="spec" value="XQ10+"/>
  <dependency type="feature" value="fullText"/>
  <test-case name="FTPrimary-FTWords-noanyall-q1a">
    <description>Tests a syntax variant of FTPrimary: literal search term with no any/all option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-noanyall-q1a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:   Test to ensure that the syntax is recognized    :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
(:   It also tests the assertion: If FTAnyallOptions :)
(:     is omitted, "any" is the default.             :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text "FTAnyallOption"
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-noanyall-q1b">
    <description>Tests a syntax variant of FTPrimary: search term expression with no any/all option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-noanyall-q1b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:   Test to ensure that the syntax is recognized    :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
(:   It also tests the assertion: If FTAnyallOptions :)
(:     is omitted, "any" is the default.             :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text { "FTAnyallOption" }
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-noanyall-q1c">
    <description>Tests a syntax variant of FTPrimary: search term expression with no any/all option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-noanyall-q1c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:   Test to ensure that the syntax is recognized    :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
(:   It also tests the assertion: If FTAnyallOptions :)
(:     is omitted, "any" is the default.             :)
declare variable $input-context external;
declare variable $search-token := "FTAnyallOption";
<paragraphs>
{ for $p in $input-context//p
where $p contains text { $search-token }
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-noanyall-q2a">
    <description>Tests a syntax variant of FTPrimary: literal search term with no any/all option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-noanyall-q2a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
(:   It also tests the assertion: If FTAnyallOptions :)
(:     is omitted, "any" is the default.             :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption specifies"
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-noanyall-q2b">
    <description>Tests a syntax variant of FTPrimary: search term expression with no any/all option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-noanyall-q2b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
(:   It also tests the assertion: If FTAnyallOptions :)
(:     is omitted, "any" is the default.             :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption literal"
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-noanyall-q2c">
    <description>Tests a syntax variant of FTPrimary: search term expression with no any/all option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-noanyall-q2c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
(:   It also tests the assertion: If FTAnyallOptions :)
(:     is omitted, "any" is the default.             :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "voting weekend"
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-noanyall-q3a">
    <description>Tests a syntax variant of FTPrimary: literal search term with no any/all option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-noanyall-q3a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
(:   It also tests the assertion: If FTAnyallOptions :)
(:     is omitted, "any" is the default.             :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption", "literal" }
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>In general, the tokens and phrases in <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> are specified using a nested XQuery expression. 
To simplify notation, the enclosing braces may be omitted if <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> consists of a single literal.
</p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-noanyall-q3b">
    <description>Tests a syntax variant of FTPrimary: search term expression with no any/all option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-noanyall-q3b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
(:   It also tests the assertion: If FTAnyallOptions :)
(:     is omitted, "any" is the default.             :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "voting", "weekend" }
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-noanyall-q4a">
    <description>Tests a syntax variant of FTPrimary: literal search term with no any/all option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-noanyall-q4a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
(:   It also tests the assertion: If FTAnyallOptions :)
(:     is omitted, "any" is the default.             :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption specifies", "voting weekend" }
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-noanyall-q4b">
    <description>Tests a syntax variant of FTPrimary: search term expression with no any/all option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-noanyall-q4b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
(:   It also tests the assertion: If FTAnyallOptions :)
(:     is omitted, "any" is the default.             :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption weekend", "voting specifies" }
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-noanyall-q4c">
    <description>Tests a syntax variant of FTPrimary: search term expression with no any/all option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-noanyall-q4c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
(:   It also tests the assertion: If FTAnyallOptions :)
(:     is omitted, "any" is the default.             :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "voting weekend", "miserable scuba diver" }
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-any-q1a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "any" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-any-q1a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text "FTAnyallOption" any
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-any-q1b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-any-q1b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text { "FTAnyallOption" } any
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-any-q1c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-any-q1c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
declare variable $input-context external;
declare variable $search-token := "FTAnyallOption";
<paragraphs>
{ for $p in $input-context//p
where $p contains text { $search-token } any
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-any-q2a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "any" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-any-q2a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption specifies" any
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-any-q2b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-any-q2b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption literal" any
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-any-q2c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-any-q2c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "voting weekend" any
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-any-q3a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "any" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-any-q3a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption", "literal" } any
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>In general, the tokens and phrases in <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> are specified using a nested XQuery expression. 
To simplify notation, the enclosing braces may be omitted if <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> consists of a single literal.
</p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-any-q3b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-any-q3b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "voting", "weekend" } any
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-any-q4a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "any" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-any-q4a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption specifies", "voting weekend" } any
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-any-q4b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-any-q4b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption weekend", "voting specifies" } any
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-any-q4c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-any-q4c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "any", the sequence of tokens for each string :)
(:     is considered as a phrase, i.e. a match is    :)
(:     found in the tokenized form of the text being :)
(:     searched, whenever that form contains a       :)
(:     subsequence of tokens that corresponds to the :)
(:     sequence of query tokens in an                :)
(:     implementation-defined way and that           :)
(:     subsequence of tokens covers consecutive      :)
(:     token positions in the tokenized text. If the :)
(:     value of the FTWordsValue contains more than  :)
(:     one string, the different strings are         :)
(:     considered to be alternatives, i.e. the       :)
(:     resulting matches must contain at least one   :)
(:     of the generated phrases.                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "voting weekend", "miserable scuba diver" } any
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-anyword-q1a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "any word" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-anyword-q1a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:   "any word", the tokens from all the strings are :)
(:   combined into a single set. The resulting       :)
(:   matches must contain at least one of the tokens :)
(:   in the set.                                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text "FTAnyallOption" any word
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-anyword-q1b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any word" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-anyword-q1b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:   "any word", the tokens from all the strings are :)
(:   combined into a single set. The resulting       :)
(:   matches must contain at least one of the tokens :)
(:   in the set.                                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text { "FTAnyallOption" } any word
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-anyword-q1c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any word" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-anyword-q1c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:   "any word", the tokens from all the strings are :)
(:   combined into a single set. The resulting       :)
(:   matches must contain at least one of the tokens :)
(:   in the set.                                     :)
declare variable $input-context external;
declare variable $search-token := "FTAnyallOption";
<paragraphs>
{ for $p in $input-context//p
where $p contains text { $search-token } any word
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-anyword-q2a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "any word" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-anyword-q2a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:   "any word", the tokens from all the strings are :)
(:   combined into a single set. The resulting       :)
(:   matches must contain at least one of the tokens :)
(:   in the set.                                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption specifies" any word
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-anyword-q2b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any word" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-anyword-q2b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:   "any word", the tokens from all the strings are :)
(:   combined into a single set. The resulting       :)
(:   matches must contain at least one of the tokens :)
(:   in the set.                                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption literal" any word
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>In general, the tokens and phrases in <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> are specified using a nested XQuery expression. 
To simplify notation, the enclosing braces may be omitted if <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> consists of a single literal.
</p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-anyword-q2c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any word" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-anyword-q2c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:   "any word", the tokens from all the strings are :)
(:   combined into a single set. The resulting       :)
(:   matches must contain at least one of the tokens :)
(:   in the set.                                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "voting weekend" any word
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-anyword-q3a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "any word" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-anyword-q3a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:   "any word", the tokens from all the strings are :)
(:   combined into a single set. The resulting       :)
(:   matches must contain at least one of the tokens :)
(:   in the set.                                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption", "literal" } any word
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>In general, the tokens and phrases in <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> are specified using a nested XQuery expression. 
To simplify notation, the enclosing braces may be omitted if <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> consists of a single literal.
</p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-anyword-q3b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any word" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-anyword-q3b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:   "any word", the tokens from all the strings are :)
(:   combined into a single set. The resulting       :)
(:   matches must contain at least one of the tokens :)
(:   in the set.                                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "voting", "weekend" } any word
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-anyword-q4a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "any word" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-anyword-q4a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:   "any word", the tokens from all the strings are :)
(:   combined into a single set. The resulting       :)
(:   matches must contain at least one of the tokens :)
(:   in the set.                                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption specifies", "voting weekend" } any word
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-anyword-q4b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any word" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-anyword-q4b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:   "any word", the tokens from all the strings are :)
(:   combined into a single set. The resulting       :)
(:   matches must contain at least one of the tokens :)
(:   in the set.                                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption weekend", "voting specifies" } any word
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-anyword-q4c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "any word" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-11-10"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-anyword-q4c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "any" "word"? | ...      :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:   "any word", the tokens from all the strings are :)
(:   combined into a single set. The resulting       :)
(:   matches must contain at least one of the tokens :)
(:   in the set.                                     :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "voting weekend", "miserable scuba diver" } any word
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-all-q1a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "all" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-all-q1a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "all" | ...              :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all", the sequence of tokens for each string :)
(:     is considered as a phrase. The resulting      :)
(:     matches must conain all of the generated      :)
(:     phrases.                                      :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text "FTAnyallOption" all
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-all-q1b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-all-q1b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" | ...              :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all", the sequence of tokens for each string :)
(:     is considered as a phrase. The resulting      :)
(:     matches must conain all of the generated      :)
(:     phrases.                                      :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text { "FTAnyallOption" } all
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-all-q1c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-all-q1c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" | ...              :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all", the sequence of tokens for each string :)
(:     is considered as a phrase. The resulting      :)
(:     matches must conain all of the generated      :)
(:     phrases.                                      :)
declare variable $input-context external;
declare variable $search-token := "FTAnyallOption";
<paragraphs>
{ for $p in $input-context//p
where $p contains text { $search-token } all
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-all-q2a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "all" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-all-q2a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "all" | ...              :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all", the sequence of tokens for each string :)
(:     is considered as a phrase. The resulting      :)
(:     matches must conain all of the generated      :)
(:     phrases.                                      :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption specifies" all
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-all-q2b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-all-q2b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "all" | ...              :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all", the sequence of tokens for each string :)
(:     is considered as a phrase. The resulting      :)
(:     matches must conain all of the generated      :)
(:     phrases.                                      :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption literal" all
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-all-q2c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-all-q2c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "all" | ...              :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all", the sequence of tokens for each string :)
(:     is considered as a phrase. The resulting      :)
(:     matches must conain all of the generated      :)
(:     phrases.                                      :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "voting weekend" all
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-all-q3a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "all" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-all-q3a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "all" | ...              :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all", the sequence of tokens for each string :)
(:     is considered as a phrase. The resulting      :)
(:     matches must conain all of the generated      :)
(:     phrases.                                      :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption", "containment" } all
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-all-q3b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-all-q3b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" | ...              :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all", the sequence of tokens for each string :)
(:     is considered as a phrase. The resulting      :)
(:     matches must conain all of the generated      :)
(:     phrases.                                      :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyAllOption", "weekend" } all
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-all-q4a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "all" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-all-q4a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" | ...              :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all", the sequence of tokens for each string :)
(:     is considered as a phrase. The resulting      :)
(:     matches must conain all of the generated      :)
(:     phrases.                                      :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption specifies", "tokens and phrases" } all
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-all-q4b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-all-q4b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" | ...              :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all", the sequence of tokens for each string :)
(:     is considered as a phrase. The resulting      :)
(:     matches must conain all of the generated      :)
(:     phrases.                                      :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "at least one", "tokens and phrases" } all
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-all-q4c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-all-q4c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" | ...              :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all", the sequence of tokens for each string :)
(:     is considered as a phrase. The resulting      :)
(:     matches must conain all of the generated      :)
(:     phrases.                                      :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "voting weekend", "miserable scuba diver" } all
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-allwords-q1a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "all words" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-allwords-q1a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "all" "words"? | ...     :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all words", the sequence of tokens from all  :)
(:     strings are combined into a single set.  The  :)
(:     resulting matches must contain all of the     :)
(:     tokens in the set.                            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text "FTAnyallOption" all words
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-allwords-q1b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all words" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-allwords-q1b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" "words"? | ...     :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all words", the sequence of tokens from all  :)
(:     strings are combined into a single set.  The  :)
(:     resulting matches must contain all of the     :)
(:     tokens in the set.                            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text { "FTAnyallOption" } all words
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-allwords-q1c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all words" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-allwords-q1c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" "words"? | ...     :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all words", the sequence of tokens from all  :)
(:     strings are combined into a single set.  The  :)
(:     resulting matches must contain all of the     :)
(:     tokens in the set.                            :)
declare variable $input-context external;
declare variable $search-token := "FTAnyallOption";
<paragraphs>
{ for $p in $input-context//p
where $p contains text { $search-token } all words
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-allwords-q2a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "all words" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-allwords-q2a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "all" "words"? | ...     :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all words", the sequence of tokens from all  :)
(:     strings are combined into a single set.  The  :)
(:     resulting matches must contain all of the     :)
(:     tokens in the set.                            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption specifies" all words
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-allwords-q2b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all words" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-allwords-q2b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "all" "words"? | ...     :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all words", the sequence of tokens from all  :)
(:     strings are combined into a single set.  The  :)
(:     resulting matches must contain all of the     :)
(:     tokens in the set.                            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption literal" all words
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-allwords-q2c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all words" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-allwords-q2c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "all" "words"? | ...     :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all words", the sequence of tokens from all  :)
(:     strings are combined into a single set.  The  :)
(:     resulting matches must contain all of the     :)
(:     tokens in the set.                            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "voting weekend" all words
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-allwords-q3a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "all words" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-allwords-q3a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( "all" "words"? | ...     :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all words", the sequence of tokens from all  :)
(:     strings are combined into a single set.  The  :)
(:     resulting matches must contain all of the     :)
(:     tokens in the set.                            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption", "literal" } all words
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-allwords-q3b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all words" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-allwords-q3b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" "words"? | ...     :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all words", the sequence of tokens from all  :)
(:     strings are combined into a single set.  The  :)
(:     resulting matches must contain all of the     :)
(:     tokens in the set.                            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "voting", "weekend" } all words
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-allwords-q4a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "all words" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-allwords-q4a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" "words"? | ...     :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all words", the sequence of tokens from all  :)
(:     strings are combined into a single set.  The  :)
(:     resulting matches must contain all of the     :)
(:     tokens in the set.                            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption matches", "tokens phrases" } all words
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-allwords-q4b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all words" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-allwords-q4b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" "words"? | ...     :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all words", the sequence of tokens from all  :)
(:     strings are combined into a single set.  The  :)
(:     resulting matches must contain all of the     :)
(:     tokens in the set.                            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption weekend", "voting specifies" } all words
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-allwords-q4c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "all words" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-allwords-q4c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( "all" "words"? | ...     :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "all words", the sequence of tokens from all  :)
(:     strings are combined into a single set.  The  :)
(:     resulting matches must contain all of the     :)
(:     tokens in the set.                            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "voting weekend", "miserable scuba diver" } all words
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-phrase-q1a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "phrase" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-phrase-q1a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( ... | "phrase"           :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "phrase", the tokens from all the strings are :)
(:     concatenated in a single sequence, which is   :)
(:     considered as a phrase. The resulting matches :)
(:     must contain the generated phrase.            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text "FTAnyallOption" phrase
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-phrase-q1b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "phrase" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-phrase-q1b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( ... | "phrase"           :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "phrase", the tokens from all the strings are :)
(:     concatenated in a single sequence, which is   :)
(:     considered as a phrase. The resulting matches :)
(:     must contain the generated phrase.            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//p
where $p contains text { "FTAnyallOption" } phrase
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-phrase-q1c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "phrase" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-phrase-q1c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( ... | "phrase"           :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "phrase", the tokens from all the strings are :)
(:     concatenated in a single sequence, which is   :)
(:     considered as a phrase. The resulting matches :)
(:     must contain the generated phrase.            :)
declare variable $input-context external;
declare variable $search-token := "FTAnyallOption";
<paragraphs>
{ for $p in $input-context//p
where $p contains text { $search-token } phrase
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p><p>The following rules specify how an <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt>
matches tokens and phrases. First, the 
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> is converted to a sequence of
strings as though it were an argument to a function with the expected
type of <code>xs:string*</code>. Then, each of those strings is tokenized into a
sequence of tokens as 
described in <loc xmlns:xlink="http://www.w3.org/1999/xlink" href="#TokenizationSec" xlink:type="simple" xlink:show="replace" xlink:actuate="onRequest">Section 4.1 Tokenization</loc>.
Then, <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is checked.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any", the sequence of tokens for each string is
considered as a phrase, i.e. a match is found in the tokenized form of 
the text being searched, whenever that form contains a subsequence of tokens
that 
corresponds to the sequence of query tokens in an implementation-defined
way and that subsequence of tokens covers consecutive token positions in 
the tokenized text. If the value of the FTWordsValue contains more 
than one string, 
the different strings are considered to be alternatives, i.e.  the resulting 
matches must contain at least one of the generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all", the sequence of tokens for each string is
considered as a phrase. The resulting matches must contain all of the 
generated phrases.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "phrase", the tokens from all the strings are
concatenated in a single sequence, which is considered as a phrase. The
resulting matches must contain the generated phrase.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "any word", the tokens from all the strings are
combined into a single set. The resulting matches must contain at least
one of the tokens in the set.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is "all words", the tokens from all the strings are
combined into a single set. The resulting matches must contain all
of the tokens in the set.</p><p>If the <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> evaluates to
a single string, the use of "any", "all", and "phrase" in
<nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> produces the same
results.</p><p>If <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> is omitted, "any" is 
the default.</p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-phrase-q2a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "phrase" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-phrase-q2a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( ... | "phrase"           :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "phrase", the tokens from all the strings are :)
(:     concatenated in a single sequence, which is   :)
(:     considered as a phrase. The resulting matches :)
(:     must contain the generated phrase.            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption specifies" phrase
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-phrase-q2b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "phrase" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-phrase-q2b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( ... | "phrase"           :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "phrase", the tokens from all the strings are :)
(:     concatenated in a single sequence, which is   :)
(:     considered as a phrase. The resulting matches :)
(:     must contain the generated phrase.            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "FTAnyallOption literal" phrase
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-phrase-q2c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "phrase" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-phrase-q2c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( ... | "phrase"           :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "phrase", the tokens from all the strings are :)
(:     concatenated in a single sequence, which is   :)
(:     considered as a phrase. The resulting matches :)
(:     must contain the generated phrase.            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text "voting weekend" phrase
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-phrase-q3a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "phrase" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-phrase-q3a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= Literal | ...                :)
(:     FTAnyAllOption ::= ( ... | "phrase"           :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "phrase", the tokens from all the strings are :)
(:     concatenated in a single sequence, which is   :)
(:     considered as a phrase. The resulting matches :)
(:     must contain the generated phrase.            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption specifies", "how containment" } phrase
return $p }
</paragraphs>(: Stylus Studio meta-information - (c) 2004-2008. Progress Software Corporation. All rights reserved.
<metaInformation>
  <scenarios>
    <scenario default="yes" name="Scenario1" userelativepaths="yes" externalpreview="no" useresolver="yes" url="" outputurl="" processortype="datadirect" tcpport="0" profilemode="0" profiledepth="" profilelength="" urlprofilexml="" commandline=""
              additionalpath="" additionalclasspath="" postprocessortype="none" postprocesscommandline="" postprocessadditionalpath="" postprocessgeneratedext="" host="" port="0" user="" password="" validateoutput="no" validator="internal"
              customvalidator=""/>
  </scenarios>
  <MapperMetaTag>
    <MapperInfo srcSchemaPathIsRelative="yes" srcSchemaInterpretAsXML="no" destSchemaPath="" destSchemaRoot="" destSchemaPathIsRelative="yes" destSchemaInterpretAsXML="no"/>
    <MapperBlockPosition></MapperBlockPosition>
    <TemplateContext></TemplateContext>
    <MapperFilter side="source"></MapperFilter>
  </MapperMetaTag>
</metaInformation>
:)]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-phrase-q3b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "phrase" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-phrase-q3b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( ... | "phrase"           :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "phrase", the tokens from all the strings are :)
(:     concatenated in a single sequence, which is   :)
(:     considered as a phrase. The resulting matches :)
(:     must contain the generated phrase.            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyAllOption", "literal" } phrase
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-phrase-q4a">
    <description>Tests a syntax variant of FTPrimary: literal search term with "phrase" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-phrase-q4a"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( ... | "phrase"           :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "phrase", the tokens from all the strings are :)
(:     concatenated in a single sequence, which is   :)
(:     considered as a phrase. The resulting matches :)
(:     must contain the generated phrase.            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTWordsValue specifies", "the tokens and phrases" } phrase
return $p }
</paragraphs>(: Stylus Studio meta-information - (c) 2004-2008. Progress Software Corporation. All rights reserved.
<metaInformation>
  <scenarios>
    <scenario default="yes" name="Scenario1" userelativepaths="yes" externalpreview="no" useresolver="yes" url="" outputurl="" processortype="datadirect" tcpport="0" profilemode="0" profiledepth="" profilelength="" urlprofilexml="" commandline=""
              additionalpath="" additionalclasspath="" postprocessortype="none" postprocesscommandline="" postprocessadditionalpath="" postprocessgeneratedext="" host="" port="0" user="" password="" validateoutput="no" validator="internal"
              customvalidator=""/>
  </scenarios>
  <MapperMetaTag>
    <MapperInfo srcSchemaPathIsRelative="yes" srcSchemaInterpretAsXML="no" destSchemaPath="" destSchemaRoot="" destSchemaPathIsRelative="yes" destSchemaInterpretAsXML="no"/>
    <MapperBlockPosition></MapperBlockPosition>
    <TemplateContext></TemplateContext>
    <MapperFilter side="source"></MapperFilter>
  </MapperMetaTag>
</metaInformation>
:)]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs><p>FTWords consists of two parts: a mandatory <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">
FTWordsValue</nt> part and an optional <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">
FTAnyallOption</nt> part. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTWordsValue" xlink:type="simple">FTWordsValue</nt> specifies the tokens and phrases
that must be contained in the matches. <nt xmlns:xlink="http://www.w3.org/1999/xlink" def="doc-xquery-FTAnyallOption" xlink:type="simple">FTAnyallOption</nt> specifies how 
containment is checked. </p></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-phrase-q4b">
    <description>Tests a syntax variant of FTPrimary: search term expression with "phrase" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-phrase-q4b"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( ... | "phrase"           :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "phrase", the tokens from all the strings are :)
(:     concatenated in a single sequence, which is   :)
(:     considered as a phrase. The resulting matches :)
(:     must contain the generated phrase.            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "FTAnyallOption and phrases", "tokens specifies" } phrase
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTPrimary-FTWords-phrase-q4c">
    <description>Tests a syntax variant of FTPrimary: search term expression with "phrase" option</description>
    <created by="Full-Text Task Force, Jim" on="2008-12-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTPrimary-FTWords-phrase-q4c"/>
    <environment>
      <source role="$input-context" file="TestSources/xpath-full-text-10.xml" declared="true"/>
    </environment>
    <test><![CDATA[(: 3.2 FTPrimary ::= FTWordsValue FTAnyAllOption?    :)
(:     FTWordsValue ::= ... | ( "{" Expr "}" )       :)
(:     FTAnyAllOption ::= ( ... | "phrase"           :)
(:   Test to ensure that the syntax is recognized    :)
(:     and that the right result is computed         :)
(:   Test assumes $input-context identifies FT spec  :)
(:   This tests the assertion: If FTAnyallOption is  :)
(:     "phrase", the tokens from all the strings are :)
(:     concatenated in a single sequence, which is   :)
(:     considered as a phrase. The resulting matches :)
(:     must contain the generated phrase.            :)
declare variable $input-context external;
<paragraphs>
{ for $p in $input-context//div2[@id="ftwords"]/p
where $p contains text { "voting weekend", "miserable scuba diver" } phrase
return $p }
</paragraphs>]]></test>
    <result>
      <assert-xml><![CDATA[<paragraphs></paragraphs>]]></assert-xml>
    </result>
  </test-case>
  <test-case name="FTWordsValue-IntegerLiteral">
    <description>Attempts to use an IntegerLiteral as an FTWordsValue</description>
    <created by="Michael Dyck" on="2010-09-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTWordsValue-IntegerLiteral"/>
    <test><![CDATA[(: Name: FTWordsValue-IntegerLiteral :)
(: Description: Attempts to use an IntegerLiteral as an FTWordsValue :)
"There are 12 months." contains text 12]]></test>
    <result>
      <error code="XPST0003"/>
    </result>
  </test-case>
  <test-case name="FTWordsValue-DecimalLiteral">
    <description>Attempts to use a DecimalLiteral as an FTWordsValue</description>
    <created by="Michael Dyck" on="2010-09-23"/>
    <modified by="Christian Gruen" on="2026-09-26" change="converted from original XQFTTS test FTWordsValue-DecimalLiteral"/>
    <test><![CDATA[(: Name: FTWordsValue-DecimalLiteral :)
(: Description: Attempts to use a DecimalLiteral as an FTWordsValue :)
"Mass of 0.454 kg." contains text 0.454]]></test>
    <result>
      <error code="XPST0003"/>
    </result>
  </test-case>
</test-set>