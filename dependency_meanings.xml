<?xml version="1.0" encoding="UTF-8"?>
<?xml-model href="dependency_meanings.sch" type="application/xml" schematypens="http://purl.oclc.org/dsdl/schematron"?>
<dependency-meanings>
	<dependency type="feature">
		<value name="advanced-uca-fallback">
			<description>
				<p>Indicates that the test uses a UCA collation URI with the option fallback=yes (or fallback omitted), and the test results assume that the implementation has a reasonably high level of support for uca collation semantics even if it is not 100% conformant.</p>
				<p>Unlike simple-uca-fallback these tests assume support for a wider range of uca collation features: including different collation strengths, different case ordering options and other options, such as numeric collations.</p>
			</description>
		</value>
		<value name="collection-stability">
			<description>
				<p>Requires collections to be stable (implementations are allowed to support non-stable collections)</p>
			</description>
		</value>
		<value name="directory-as-collection-uri">
			<description>
				<p>Requires an implementation to recognize a file:/// URL identifying a directory as a collection URI</p>
			</description>
		</value>
		<value name="fn-format-integer-CLDR">
			<description>
				<p>Indicates that the test uses an fn:format-integer call which uses CLDR numbering scheme names, e.g. -wo(%spellout-ordinal)</p>
			</description>
		</value>
		<value name="fn-load-xquery-module">
			<description>
				<p>Indicates that the test uses an fn:load-xquery-module() call which requires an XQuery processor which supports the Module Feature</p>
			</description>
		</value>
		<value name="fn-transform-XSLT">
			<description>
				<p>Indicates that the test uses an fn:transform() call which requires an XSLT processor</p>
			</description>
		</value>
		<value name="fn-transform-XSLT30">
			<description>
				<p>Indicates that the test uses an fn:transform() call which requires an XSLT 3.0 processor</p>
			</description>
		</value>
		<value name="higherOrderFunctions">
			<description>
				<p>Requires support for higher order functions (XPath or XQuery)</p>
			</description>
		</value>
		<value name="infoset-dtd">
			<description>
				<p>Requires source documents to be constructed according to the rules for construction from an Infoset, in particular recognizing ID/IDREF attributes identified as such in a DTD</p>
			</description>
		</value>
		<value name="moduleImport">
			<description>
				<p>For XQuery, requires support for import module declaration</p>
			</description>
		</value>
		<value name="namespace-axis">
			<description>
				<p>For XPath tests, requires support for the namespace axis</p>
			</description>
		</value>
		<value name="non_empty_sequence_collection">
			<description>
				<p>Indicates that the test needs the default collection to be something other than an empty sequence</p>
			</description>
		</value>
		<value name="non_unicode_codepoint_collation">
			<description>
				<p>Indicates that the test uses a collation URI other than those which the specification requires every processor to support</p>
			</description>
		</value>
		<value name="remote_http">
			<description>
				<p>Indicates that the test uses uses a function such as doc() or unparsed-text() to fetch resources from the web using an HTTP/HTTPS URL.</p>
			</description>
		</value>
		<value name="schema-location-hint">
			<description>
				<p>Requires the "at URI" location hint in import schema to be interpreted as a relative URL.</p>
			</description>
		</value>
		<value name="schemaImport">
			<description>
				<p>Requires the query to support an import schema declaration.</p>
			</description>
		</value>
		<value name="schemaValidation">
			<description>
				<p>Requires a query to support the validate expression, and/or to be able to accept source documents that have been validated.</p>
			</description>
		</value>
		<value name="serialization">
			<description>
				<p>Requires the query to support serialization: specifically, to recognize the serialization parameters defined in the query and to perform serialization using the values of those parameters as defined in the Serialization 3.0 specification (this is a stronger requirement than merely providing the XQuery 3.0 serialization feature).</p>
			</description>
		</value>
		<value name="simple-uca-fallback">
			<description>
				<p>Indicates that the test uses a UCA collation URI with the option fallback=yes (or fallback omitted), and the test results assume that the implementation falls back to a "sensible" default if UCA collation is not supported.</p>
				<p>The only assumptions made about what constitutes a "sensible" default are that the sequences 0-9, a-z, and A-Z are collated as in codepoint collation, and the option "strength=secondary" indicates case-blind matching of a-z with A-Z. The strings to be sorted will not contain any characters outside these ranges, and will not require characters in one of these ranges to be compared with characters in another. For example, a sensible default would sort "abc" before "def"; a fallback collation that sorted "def" before "abc", while legal according to the language specification, would not satisfy this dependency.</p>
			</description>
		</value>
		<value name="staticTyping">
			<description>
				<p>Requires "pessimistic" static typing</p>
			</description>
		</value>
		<value name="typedData">
			<description>
				<p>Requires the processor to support typed data (but not necessarily schema import or schema validation)</p>
			</description>
		</value>
		<value name="xpath-1.0-compatibility">
			<description>
				<p>For XPath tests, requires the expression to be evaluated in XPath 1.0 compatibility mode. (In general, if this dependency is not present then the test must NOT be run in 1.0 compatibility mode.)</p>
			</description>
		</value>
		<value name="XQUpdate">
			<description>
				<p>Requires support for XQuery Update</p>
			</description>
		</value>
		<value name="olson-timezone" />
		<value name="arbitraryPrecisionDecimal"/>
		<value name="binary"/>
		<value name="expath-file"/>
	</dependency>
	<dependency type="spec">
		<description>
			<p>A space-separated list of codes that indicate which XPath and/or XQuery versions that the test-set/case is relevant to, for example: "XQ31+ XP40".</p>
			<p>If a code ends with the plus symbol (+) it should be read as "or later version".</p>			
		</description>
	</dependency>
	<dependency type="xml-version">
		<description>
			<p>A space-separated list of the XML versions that the test-set/case is relevant to.</p>
			<p>If a version label ends with the plus symbol (+) it should be read as "or later version".</p>	
		</description>
	</dependency>
	<dependency type="xsd-version">
		<description>
			<p>A space-separated list of the XSD versions that the test-set/case is relevant to.</p>
		</description>
	</dependency>
	<dependency type="language"/>
	<dependency type="default-language"/>
	<dependency type="format-integer-sequence"/>
	<dependency type="put"/>
	<dependency type="unicode-normalization-form"/>
	<dependency type="unicode-version"/>
	<dependency type="limits" value="big_integer">
		<value name="big_integer"/>
		<value name="year_lt_0"/>
	</dependency>
	<dependency type="revalidation"/>
	<dependency type="calendar"/>
</dependency-meanings>